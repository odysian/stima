# Web framework
fastapi==0.135.3
uvicorn[standard]==0.44.0

# Database
SQLAlchemy==2.0.49
alembic==1.18.4
asyncpg==0.31.0
psycopg2-binary==2.9.11

# Validation / settings
pydantic==2.13.1
pydantic-settings==2.13.1
python-dotenv==1.2.2
email-validator==2.3.0
python-multipart==0.0.26

# Auth — PyJWT only. See exclusions table below.
PyJWT==2.12.1
argon2-cffi==25.1.0

# Rate limiting
# Latest release on PyPI as of this task; keep intentionally pinned.
slowapi==0.1.9
redis==7.4.0
arq==0.27.0
sentry-sdk[fastapi]==2.58.0

# AI
openai==2.31.0
anthropic==0.95.0

# PDF generation — WeasyPrint + Jinja2 template rendering
weasyprint==68.1
jinja2==3.1.6
google-cloud-storage==3.10.1

# Audio processing — pydub requires ffmpeg binary (see Docker section)
pydub==0.25.1
audioop-lts==0.2.2; python_version >= "3.13"

# Testing
httpx==0.28.1
pytest==9.0.3
pytest-asyncio==1.3.0
pytest-cov==7.1.0

# Linting / type checking / security
ruff==0.15.10
mypy==1.20.1
bandit==1.9.4
# Transitive via bandit -> rich; pin for CVE-2026-4539 (pygments GHSA / PyPI advisory).
Pygments==2.20.0
