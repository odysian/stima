## Improvement notes
hide suggested tax until the user clicks the tax box.

format address lines properly on pdfs
slide record button down to more user friendly thumb position
consider new flow where we start recording clips and building quote before selecting user, allows faster access (YES)

loading spinner, spin the color, not the entire object, maybe triangle shape instead?

favicon/thumbnail image for presentability

search toggle on home list — magnifying glass icon in trailing slot of the Quotes/Invoices tab row; tapping shows the search bar, × inside the bar closes it and resets query; reopening is always a fresh empty search; bar hidden by default done

landing page


extraction timeout?

single row - two buttons on review screen done

toast notification review screen draft saved actions and similar ones across app. done

glowing warning to select customer on draft review with no customer selected done
